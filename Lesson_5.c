#include <stdio.h>
#include <stdlib.h>
#include <math.h> //Needed to use math functions in VSC

int main() {

    printf("%f\n", pow(4,3));  // ^ to the power of
    printf("%f\n", sqrt(36)); //Square root
    printf("%f\n", ceil(36.356)); //Round up
    printf("%f\n", floor(36.656)); //Round Down

    return 0;
}